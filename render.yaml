services:
  - type: web
    name: arcana-suite
    env: python
    plan: starter
    runtime: python3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
